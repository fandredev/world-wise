import Spinner from '../Spinner';
import styles from './spinner-full.module.css';

export default function SpinnerFullPage() {
  return (
    <div className={styles.spinnerFullpage}>
      <Spinner />
    </div>
  );
}
